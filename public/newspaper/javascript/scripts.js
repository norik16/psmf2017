$('body').noisy({
    intensity: 0.4,
    size: 200,
    opacity: 0.05,
    fallback: 'fallback.png',
    randomColors: false, // true by default
    color: '#000000'
});

$('body').noisy({
    intensity: 0.4,
    size: 800,
    opacity: 0.05,
    fallback: 'fallback.png',
    randomColors: false, // true by default
    color: '#603020'
});

$('body').noisy({
    intensity: 0.9,
    size: 200,
    opacity: 0.05,
    fallback: 'fallback.png',
    randomColors: false, // true by default
    color: '#706040'
});
$('body').noisy({
    opacity: 0.05
});